# Example - AI Governance Proposal

The AI Estimating Assistant proposes adding a new production-rate candidate based on verified project actuals. The proposal contains source records, method, conditions, calculated rate, confidence, and target library diff. A chief estimator reviews and approves it. The system creates a new draft library version rather than modifying the locked active version.
