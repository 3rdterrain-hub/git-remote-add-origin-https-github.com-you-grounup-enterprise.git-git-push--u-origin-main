# Example - Offline Daily Report Sync

A field user downloads an encrypted project pack, records a daily report without connectivity, attaches photos, and later synchronizes. The platform validates the device, actor, tenant, base versions, and file checksums; applies non-conflicting changes; creates a conflict case for a concurrently edited note; and returns a signed sync receipt.
