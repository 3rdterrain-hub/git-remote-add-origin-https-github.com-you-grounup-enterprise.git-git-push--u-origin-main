# Mobile and Offline Specification

Mobile supports time entry, schedules, crew rosters, credentials, training, leave, safety acknowledgments and manager approvals. Offline transactions use encrypted local storage, device identity, immutable client timestamps, conflict detection, policy-based merge rules and server acknowledgment. Location data is collected only under approved policy.
