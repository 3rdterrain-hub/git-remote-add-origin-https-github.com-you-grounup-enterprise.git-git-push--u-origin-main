# Default Deny Contract
A request with no explicit granting policy is denied.
A request with an explicit deny is denied.
Only a matching valid allow policy within tenant/resource scope may permit access.
