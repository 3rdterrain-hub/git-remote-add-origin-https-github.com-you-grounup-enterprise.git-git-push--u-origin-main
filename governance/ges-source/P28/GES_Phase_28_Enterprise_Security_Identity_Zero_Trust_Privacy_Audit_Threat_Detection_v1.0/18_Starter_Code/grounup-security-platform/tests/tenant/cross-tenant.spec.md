# Cross-Tenant Isolation
Tenant A cannot access Tenant B records through:
API, direct repository, search, file metadata/object access, event subscription, cache key, export, AI retrieval, AI tools, or audit queries.
