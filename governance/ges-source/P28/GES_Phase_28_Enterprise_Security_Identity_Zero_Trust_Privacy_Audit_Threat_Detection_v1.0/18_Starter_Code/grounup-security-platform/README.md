# GrounUp Security Platform - Phase 28

Vendor-neutral security reference scaffold.

Core invariants:
- default deny
- least privilege
- explicit tenant scope
- privileged access is temporary/approved/strongly authenticated
- secrets/keys are references to managed systems, never embedded values
- security audit history is protected
- AI tools and retrieval inherit ordinary identity/data policy
