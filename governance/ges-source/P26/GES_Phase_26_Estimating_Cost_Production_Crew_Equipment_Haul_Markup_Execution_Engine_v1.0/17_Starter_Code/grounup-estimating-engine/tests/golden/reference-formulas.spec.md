# Golden Formula Tests
- duration = quantity / production
- labor hours = duration * crew count
- trips = ceil(quantity / payload)
- truck count = ceil(source production * cycle hours / payload)
- material purchase = installed * (1 + waste)
- unit sell = sell / priced quantity
Use fixed controlled fixtures and compare exact/approved rounded outputs.
