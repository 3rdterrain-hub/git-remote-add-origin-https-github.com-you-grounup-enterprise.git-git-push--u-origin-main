# GrounUp Estimating Engine - Phase 26

Vendor/framework-neutral deterministic calculation scaffold.

Core invariants:
- every authoritative calculation references an immutable library snapshot
- every result carries formula/rule/input/version lineage
- trip-based haul and hourly haul are explicit modes
- equipment rate-period choice is policy-driven
- AI is advisory only
- approved/frozen estimate versions are immutable
