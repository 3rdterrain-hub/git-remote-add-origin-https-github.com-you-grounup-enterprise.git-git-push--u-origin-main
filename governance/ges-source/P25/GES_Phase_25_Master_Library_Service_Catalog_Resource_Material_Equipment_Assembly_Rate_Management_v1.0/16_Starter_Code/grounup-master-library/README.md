# GrounUp Master Library Service - Phase 25

Vendor/framework-neutral reference implementation scaffold.

Core invariants:
- published versions are immutable
- tenant overrides never erase base provenance
- estimator snapshots are immutable
- AI proposals require human approval before becoming governed library versions
- units, effective dates, source, geography, currency and approval are explicit
