# Traceability

Each controlled requirement maps to a unique primary test case in `P07_Traceability.csv`. Design artifacts use stable identifiers for requirements, business rules, validation rules, entities, workflows, engines, APIs, UI surfaces, reports, KPIs, and tests.