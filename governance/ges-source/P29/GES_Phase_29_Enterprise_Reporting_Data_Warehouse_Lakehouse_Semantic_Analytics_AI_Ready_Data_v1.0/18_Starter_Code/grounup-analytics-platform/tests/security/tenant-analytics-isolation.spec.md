# Tenant Analytics Isolation
Tenant A cannot access Tenant B through raw zones, warehouse facts, semantic model, dashboard, report, export, benchmark-identifiable output, feature snapshot, embedding metadata, or AI analytics query.
