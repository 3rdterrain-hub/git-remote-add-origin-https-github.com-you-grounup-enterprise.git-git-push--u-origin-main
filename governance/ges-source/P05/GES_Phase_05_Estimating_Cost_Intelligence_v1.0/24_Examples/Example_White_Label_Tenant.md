# Example - White-Label Tenant

An OEM tenant uses its own brand, verified domain, email sender, mobile icon, proposal templates and terminology while inheriting the GrounUp calculation engine and approved master libraries. Local overrides remain versioned and auditable.
